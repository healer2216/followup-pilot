# AI 出院随访管家 — 技术设计书

> 项目代号：FollowUp Pilot  
> 版本：v2.0（深度版）  
> 关联文档：PRD-需求开发文档.md

---

## 1. 系统架构总览

### 1.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                     展示层 (Streamlit)                        │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────────┐     │
│  │ Page:上传 │  │ Page:计划展示 │  │ Page:对话随访       │     │
│  └─────┬────┘  └──────┬───────┘  └─────────┬──────────┘     │
└────────┼──────────────┼────────────────────┼────────────────┘
         │ HTTP         │ HTTP               │ HTTP
┌────────▼──────────────▼────────────────────▼────────────────┐
│                   服务层 (FastAPI)                             │
│                                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌────────────────────────┐ │
│  │ ParserSvc   │ │ EvidenceSvc │ │ PlannerSvc             │ │
│  │ 出院小结解析 │ │ 循证检索     │ │ 随访计划生成            │ │
│  └──────┬──────┘ └──────┬──────┘ └──────────┬─────────────┘ │
│         │               │                    │               │
│  ┌──────▼───────────────▼────────────────────▼─────────────┐ │
│  │                  ChatbotSvc 对话随访引擎                  │ │
│  │  - 状态管理（会话上下文 + 随访计划）                       │ │
│  │  - 预警评估（规则引擎 + LLM 辅助判断）                    │ │
│  └──────────────────────────┬──────────────────────────────┘ │
└─────────────────────────────┼───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    基础设施层                                  │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────────────┐  │
│  │ LLM Gateway│  │ KnowS Client │  │ SessionStore (JSON) │  │
│  │ OpenAI兼容  │  │ 循证检索客户端│  │ 会话持久化           │  │
│  └────────────┘  └──────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 请求流转（核心链路）

**链路 A：上传 → 解析 → 检索 → 生成计划（一次性）**
```
用户上传文本
  → ParserSvc.parse(text)          # LLM 结构化提取
  → EvidenceSvc.search(structured) # KnowS 多源串行检索
  → PlannerSvc.generate(structured, evidences)  # LLM 融合生成计划
  → 返回 FollowUpPlan
```

**链路 B：对话随访（多轮）**
```
用户发消息
  → ChatbotSvc.chat(session_id, message)
    → 加载会话上下文（plan + history）
    → 如需补充证据 → EvidenceSvc.search_on_demand(message)
    → LLM 生成回复 + 预警等级
    → 更新会话状态
  → 返回 ChatResponse
```

---

## 2. 数据模型设计

### 2.1 核心模型（Pydantic）

```python
# ============================================================
# app/models.py — 全局数据模型
# ============================================================
from __future__ import annotations
from datetime import datetime
from enum import Enum
from typing import Any, Optional
from pydantic import BaseModel, Field


# ---------- 枚举 ----------

class EvidenceSource(str, Enum):
    """KnowS 支持的 6 类证据源"""
    PAPER_EN = "paper_en"
    PAPER_CN = "paper_cn"
    MEETING = "meeting"
    GUIDE = "guide"
    TRIAL = "trial"
    PACKAGE_INSERT = "package_insert"


class AlertLevel(str, Enum):
    GREEN = "green"
    YELLOW = "yellow"
    RED = "red"


class TaskType(str, Enum):
    MEDICATION = "medication"       # 服药
    MONITORING = "monitoring"       # 监测指标
    APPOINTMENT = "appointment"     # 复查
    DIET = "diet"                   # 饮食
    EXERCISE = "exercise"           # 运动
    SYMPTOM_CHECK = "symptom_check" # 症状自查


# ---------- M1: 出院小结解析结果 ----------

class Medication(BaseModel):
    name: str
    dosage: str = ""
    frequency: str = ""
    purpose: str = ""


class DischargeSummary(BaseModel):
    """出院小结结构化结果"""
    patient_name: str = ""
    age: Optional[int] = None
    gender: str = ""
    diagnosis: str                              # 主诊断，逗号分隔
    diagnosis_list: list[str] = Field(default_factory=list)  # 拆分后的诊断列表
    hospitalization_dates: dict[str, str] = Field(default_factory=dict)
    procedures: list[str] = Field(default_factory=list)
    medications: list[Medication] = Field(default_factory=list)
    precautions: list[str] = Field(default_factory=list)
    followup_recommendation: str = ""
    lab_results: dict[str, str] = Field(default_factory=dict)


# ---------- M2: 循证证据 ----------

class Evidence(BaseModel):
    """单条循证证据"""
    id: str
    source: EvidenceSource
    title: str
    abstract: Optional[str] = None
    publish_date: Optional[str] = None
    journal: Optional[str] = None
    doi: Optional[str] = None
    study_type: Optional[str] = None
    impact_factor: Optional[float] = None
    has_pdf: bool = False
    organizations: list[str] = Field(default_factory=list)


class EvidenceRef(BaseModel):
    """计划/对话中引用的证据摘要（轻量版）"""
    source: EvidenceSource
    title: str
    snippet: str           # 引用的关键片段
    evidence_id: str = ""  # 关联到完整 Evidence


# ---------- M3: 随访计划 ----------

class FollowUpTask(BaseModel):
    """随访计划中的单个任务"""
    type: TaskType
    title: str
    detail: str
    target: Optional[str] = None              # 目标值（如血糖范围）
    evidence: Optional[EvidenceRef] = None    # 循证来源


class TimelineDay(BaseModel):
    """时间线中的某一天/某一周"""
    day: int                   # 出院后第几天（1=出院当天）
    label: str                 # 展示标签："出院第1天" / "出院第1周"
    tasks: list[FollowUpTask] = Field(default_factory=list)


class FollowUpPlan(BaseModel):
    """完整随访计划"""
    plan_id: str
    created_at: datetime = Field(default_factory=datetime.now)
    diagnosis_summary: str
    timeline: list[TimelineDay] = Field(default_factory=list)
    evidence_pool: list[Evidence] = Field(default_factory=list)  # 检索到的所有证据
    evidence_count: int = 0
    cited_count: int = 0


# ---------- M4: 对话随访 ----------

class ChatMessage(BaseModel):
    role: str                 # "user" | "assistant" | "system"
    content: str
    timestamp: datetime = Field(default_factory=datetime.now)
    alert_level: Optional[AlertLevel] = None
    evidence_refs: list[EvidenceRef] = Field(default_factory=list)
    suggest_actions: list[str] = Field(default_factory=list)


class ChatSession(BaseModel):
    """对话会话状态"""
    session_id: str
    plan: Optional[FollowUpPlan] = None
    discharge_summary: Optional[DischargeSummary] = None
    messages: list[ChatMessage] = Field(default_factory=list)
    current_alert: AlertLevel = AlertLevel.GREEN
    alert_history: list[dict] = Field(default_factory=list)  # [{timestamp, level, detail}]
    created_at: datetime = Field(default_factory=datetime.now)


# ---------- API 请求/响应 ----------

class ParseRequest(BaseModel):
    text: str

class ParseResponse(BaseModel):
    structured: DischargeSummary
    raw_text: str
    confidence: float = 0.0

class PlanRequest(BaseModel):
    structured: DischargeSummary

class PlanResponse(BaseModel):
    plan: FollowUpPlan
    evidence_summary: str

class ChatRequest(BaseModel):
    session_id: str
    message: str

class ChatResponse(BaseModel):
    reply: str
    alert_level: AlertLevel
    alert_detail: Optional[str] = None
    evidence_refs: list[EvidenceRef] = Field(default_factory=list)
    suggest_actions: list[str] = Field(default_factory=list)

class HealthResponse(BaseModel):
    status: str = "ok"
    knows_api: bool = True
    llm_api: bool = True
```

### 2.2 数据流图

```
                    ┌──────────────┐
                    │  出院小结文本  │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  ParserSvc   │ ← LLM (JSON Mode)
                    └──────┬───────┘
                           │
                    ┌──────▼───────────┐
                    │ DischargeSummary │
                    └──┬────────────┬──┘
                       │            │
                ┌──────▼──────┐     │
                │ EvidenceSvc │     │
                │ (KnowS API) │     │
                └──────┬──────┘     │
                       │            │
                ┌──────▼──────┐     │
                │ Evidence[]  │     │
                └──────┬──────┘     │
                       │            │
                ┌──────▼────────────▼──┐
                │      PlannerSvc      │ ← LLM
                └──────┬───────────────┘
                       │
                ┌──────▼──────┐
                │ FollowUpPlan│
                └──────┬──────┘
                       │
                ┌──────▼──────┐
                │ ChatbotSvc  │ ← LLM + 规则引擎
                └─────────────┘
```

---

## 3. 模块详细设计

### 3.1 M1：出院小结解析服务 (parser.py)

**职责**：接收原始文本，通过 LLM 提取结构化 DischargeSummary。

```python
# app/services/parser.py

PARSER_SYSTEM_PROMPT = """你是一个专业的医疗文书解析助手。
你的任务是从出院小结中提取结构化信息。

请严格按照以下 JSON 格式输出，不要添加任何额外文字：
{
  "patient_name": "患者姓名",
  "age": 年龄数字,
  "gender": "男/女",
  "diagnosis": "主要诊断（原文）",
  "diagnosis_list": ["诊断1", "诊断2"],
  "hospitalization_dates": {"admission": "YYYY-MM-DD", "discharge": "YYYY-MM-DD"},
  "procedures": ["手术/操作名称"],
  "medications": [
    {"name": "药名", "dosage": "剂量", "frequency": "频次", "purpose": "用途"}
  ],
  "precautions": ["注意事项1", "注意事项2"],
  "followup_recommendation": "随访建议原文",
  "lab_results": {"指标名": "值 单位"}
}

规则：
1. 如果某字段在文中找不到对应信息，填空字符串或空列表
2. diagnosis_list 需拆分为独立诊断项
3. medications 中 purpose 如果文中未明确，可根据药物推断
4. 日期格式统一为 YYYY-MM-DD，如果原文格式不同请转换
5. lab_results 的 key 用英文指标名，value 保留原始数值和单位
"""

class ParserService:
    """出院小结解析服务"""

    def __init__(self, llm_client):
        self.llm = llm_client

    async def parse(self, text: str) -> tuple[DischargeSummary, float]:
        """
        解析出院小结文本。

        Returns:
            (structured_data, confidence_score)
        """
        messages = [
            {"role": "system", "content": PARSER_SYSTEM_PROMPT},
            {"role": "user", "content": f"请解析以下出院小结：\n\n{text}"}
        ]

        response = await self.llm.chat_completion(
            messages=messages,
            response_format={"type": "json_object"},
            temperature=0.1  # 低温度，保证稳定输出
        )

        raw_json = json.loads(response["content"])
        structured = DischargeSummary(**raw_json)
        confidence = self._calculate_confidence(structured, text)

        return structured, confidence

    def _calculate_confidence(self, data: DischargeSummary, text: str) -> float:
        """简单置信度评估：核心字段非空即加分"""
        score = 0.0
        if data.diagnosis: score += 0.3
        if data.medications: score += 0.3
        if data.followup_recommendation: score += 0.2
        if data.lab_results: score += 0.1
        if data.patient_name: score += 0.1
        return min(score, 1.0)
```

### 3.2 M2：循证检索服务 (evidence.py)

**职责**：根据结构化数据自动构造查询，串行调用 KnowS API。

```python
# app/services/evidence.py

class EvidenceService:
    """KnowS 循证检索服务"""

    SERIAL_INTERVAL = 0.4  # 串行间隔（秒），满足 3 QPS

    def __init__(self, knows_client):
        self.knows = knows_client

    async def search_for_plan(
        self, summary: DischargeSummary
    ) -> list[Evidence]:
        """
        根据出院小结自动构造多源查询，串行检索。

        查询策略：
        1. guide — 查该病种的随访指南（最高优先级）
        2. package_insert — 查每种药物的说明书
        3. paper_cn — 查中文论文（恢复期管理）
        4. paper_en — 查英文论文（最新循证证据）
        """
        queries = self._build_queries(summary)
        all_evidences: list[Evidence] = []

        for i, (source, query) in enumerate(queries):
            if i > 0:
                await asyncio.sleep(self.SERIAL_INTERVAL)
            try:
                results = await self.knows.search(source, query)
                all_evidences.extend(results)
            except Exception as e:
                logger.warning(f"KnowS search failed for {source}: {e}")
                continue  # 单源失败不阻断整体

        return all_evidences

    async def search_on_demand(
        self, query: str, sources: list[EvidenceSource] = None
    ) -> list[Evidence]:
        """
        对话中按需补充检索（M4 ChatbotSvc 调用）。
        当患者提到新症状/问题时，动态检索相关证据。
        """
        if sources is None:
            sources = [EvidenceSource.GUIDE, EvidenceSource.PAPER_CN]

        results = []
        for i, source in enumerate(sources):
            if i > 0:
                await asyncio.sleep(self.SERIAL_INTERVAL)
            try:
                evs = await self.knows.search(source, query)
                results.extend(evs[:3])  # 按需检索取 Top 3
            except Exception:
                continue
        return results

    def _build_queries(
        self, summary: DischargeSummary
    ) -> list[tuple[EvidenceSource, str]]:
        """自动构造查询列表"""
        queries = []
        diagnosis = summary.diagnosis

        # 1. 临床指南：病种 + 随访/管理/出院
        queries.append((
            EvidenceSource.GUIDE,
            f"{diagnosis} 出院后随访管理"
        ))

        # 2. 药品说明书：每种药物单独查
        for med in summary.medications:
            queries.append((
                EvidenceSource.PACKAGE_INSERT,
                f"{med.name} 注意事项 不良反应"
            ))

        # 3. 中文论文：病种 + 出院 + 随访
        queries.append((
            EvidenceSource.PAPER_CN,
            f"{diagnosis} 出院后 随访 康复 管理"
        ))

        # 4. 英文论文：英文诊断 + follow-up
        # 简单处理：用中文诊断（KnowS 内部会做翻译）
        queries.append((
            EvidenceSource.PAPER_EN,
            f"{diagnosis} post-discharge follow-up management guidelines"
        ))

        return queries
```

### 3.3 M3：随访计划生成服务 (planner.py)

**职责**：融合结构化数据 + 循证证据，生成时间线式随访计划。

```python
# app/services/planner.py

PLANNER_SYSTEM_PROMPT = """你是一个专业的临床随访计划制定助手。
你需要根据患者的出院信息和循证证据，制定个性化的随访计划。

## 输入
1. 患者出院小结（结构化 JSON）
2. 循证证据列表（来自医学指南、论文、药品说明书）

## 输出要求
生成 JSON 格式的随访计划，包含时间线（出院后第1天、第3天、第1周、第2周、第1月）。

每个时间节点包含若干任务，每个任务必须：
- 标注类型（medication/monitoring/appointment/diet/exercise/symptom_check）
- 给出具体可执行的描述
- 引用循证证据（如果有相关证据）

## JSON 格式
{
  "timeline": [
    {
      "day": 1,
      "label": "出院第1天",
      "tasks": [
        {
          "type": "medication",
          "title": "任务标题",
          "detail": "具体描述",
          "target": "目标值（如有）",
          "evidence": {
            "source": "guide",
            "title": "引用的证据标题",
            "snippet": "证据中的关键原文",
            "evidence_id": "证据ID"
          }
        }
      ]
    }
  ]
}

## 规则
1. 优先使用循证证据，无证据时基于临床常识但要标注
2. 任务描述要通俗易懂，患者能直接执行
3. 药物类任务必须引用说明书
4. 复查类任务必须引用指南
5. 每个时间节点 2-4 个任务，不要过多
6. target 字段用于可量化的指标（如血糖范围、血压范围）
"""


class PlannerService:
    """随访计划生成服务"""

    def __init__(self, llm_client):
        self.llm = llm_client

    async def generate(
        self,
        summary: DischargeSummary,
        evidences: list[Evidence],
    ) -> FollowUpPlan:
        """
        融合患者数据 + 循证证据 → 生成随访计划。
        """
        # 构造 prompt
        evidence_text = self._format_evidences(evidences)
        user_content = (
            f"## 患者出院小结\n{summary.model_dump_json(indent=2)}\n\n"
            f"## 循证证据\n{evidence_text}"
        )

        messages = [
            {"role": "system", "content": PLANNER_SYSTEM_PROMPT},
            {"role": "user", "content": user_content},
        ]

        response = await self.llm.chat_completion(
            messages=messages,
            response_format={"type": "json_object"},
            temperature=0.3,
        )

        raw = json.loads(response["content"])
        timeline = [TimelineDay(**day) for day in raw.get("timeline", [])]

        # 统计引用数
        cited_ids = set()
        for day in timeline:
            for task in day.tasks:
                if task.evidence and task.evidence.evidence_id:
                    cited_ids.add(task.evidence.evidence_id)

        plan = FollowUpPlan(
            plan_id=f"plan_{datetime.now().strftime('%Y%m%d%H%M%S')}",
            diagnosis_summary=summary.diagnosis,
            timeline=timeline,
            evidence_pool=evidences,
            evidence_count=len(evidences),
            cited_count=len(cited_ids),
        )

        return plan

    def _format_evidences(self, evidences: list[Evidence]) -> str:
        """将证据列表格式化为 LLM 可读的文本"""
        lines = []
        for i, ev in enumerate(evidences, 1):
            lines.append(
                f"[{i}] ID:{ev.id} | 来源:{ev.source.value} | "
                f"标题:{ev.title}\n"
                f"    摘要:{(ev.abstract or '无')[:200]}"
            )
        return "\n".join(lines)
```

### 3.4 M4：对话随访引擎 (chatbot.py)

**职责**：管理对话会话，结合随访计划评估患者状态，输出预警。

```python
# app/services/chatbot.py

CHATBOT_SYSTEM_PROMPT = """你是一个专业且温暖的 AI 随访护士。
你的任务是根据患者的随访计划，评估他们的恢复情况。

## 患者信息
- 诊断：{diagnosis}
- 当前用药：{medications}
- 随访计划中的关注点：{focus_points}

## 你的行为准则
1. 语气温暖但专业，像一位关心的护士
2. 主动询问关键指标（血糖/血压/症状等）
3. 对患者的回答做出评估：
   - 指标正常 → 肯定并鼓励
   - 轻度异常 → 给出调整建议
   - 严重异常 → 明确建议就医
4. 每次回复都引用循证依据
5. 不要给出具体的药物调整建议（那是医生的职责）
6. 末尾总是问一个后续问题，引导患者继续交流

## 预警规则（内置，不需要你判断，系统会处理）
- 血糖 > 13.9 mmol/L 或 < 3.9 mmol/L → 红色预警
- 血压 > 180/110 mmHg → 红色预警
- 胸痛、呼吸困难、意识模糊 → 红色预警
- 血糖 7.0-13.9 或 < 4.4 mmol/L → 黄色关注
- 血压 140-180/90-110 mmHg → 黄色关注

## 回复格式
请用自然语言回复，在关键建议后用方括号标注循证来源：
[来源：XXX指南/说明书 - 关键原文摘要]

## 免责声明
每次回复末尾附上：
---
*以上建议仅供参考，不替代专业医嘱。如症状加重，请及时就医。*
"""


class AlertEngine:
    """预警规则引擎（独立于 LLM，保证可靠性）"""

    # 红色预警阈值
    RED_THRESHOLDS = {
        "fasting_glucose": {"high": 13.9, "low": 3.9, "unit": "mmol/L"},
        "blood_pressure_systolic": {"high": 180, "unit": "mmHg"},
        "blood_pressure_diastolic": {"high": 110, "unit": "mmHg"},
    }

    # 黄色关注阈值
    YELLOW_THRESHOLDS = {
        "fasting_glucose": {"high": 10.0, "low": 4.4, "unit": "mmol/L"},
        "blood_pressure_systolic": {"high": 140, "unit": "mmHg"},
        "blood_pressure_diastolic": {"high": 90, "unit": "mmHg"},
    }

    # 关键词触发红色预警
    RED_KEYWORDS = [
        "胸痛", "胸闷", "呼吸困难", "意识模糊", "昏迷",
        "大量出血", "剧烈头痛", "抽搐", "过敏性休克"
    ]

    def evaluate(self, message: str, extracted_values: dict) -> tuple[AlertLevel, Optional[str]]:
        """
        评估消息中的指标值 + 关键词。

        Args:
            message: 患者原始消息
            extracted_values: LLM 从消息中提取的数值 {"fasting_glucose": 6.8, ...}

        Returns:
            (alert_level, alert_detail)
        """
        # 1. 关键词检查（最高优先级）
        for keyword in self.RED_KEYWORDS:
            if keyword in message:
                return AlertLevel.RED, f"检测到危险症状关键词「{keyword}」，建议立即就医"

        # 2. 数值检查
        alert_level = AlertLevel.GREEN
        detail = None

        for key, value in extracted_values.items():
            if not isinstance(value, (int, float)):
                continue

            # 红色阈值
            if key in self.RED_THRESHOLDS:
                t = self.RED_THRESHOLDS[key]
                if value >= t.get("high", float("inf")) or value <= t.get("low", 0):
                    return AlertLevel.RED, f"{key} = {value} {t['unit']}，超出安全范围"

            # 黄色阈值
            if key in self.YELLOW_THRESHOLDS:
                t = self.YELLOW_THRESHOLDS[key]
                if value >= t.get("high", float("inf")) or value <= t.get("low", 0):
                    if alert_level != AlertLevel.RED:
                        alert_level = AlertLevel.YELLOW
                        detail = f"{key} = {value} {t['unit']}，需要关注"

        return alert_level, detail


class ChatbotService:
    """对话随访引擎"""

    def __init__(self, llm_client, evidence_service: EvidenceService):
        self.llm = llm_client
        self.evidence = evidence_service
        self.alert_engine = AlertEngine()

    async def chat(
        self,
        session: ChatSession,
        message: str,
    ) -> ChatResponse:
        """
        处理一轮对话。

        流程：
        1. LLM 提取消息中的数值指标
        2. 规则引擎评估预警等级
        3. 如需补充证据 → 调用 KnowS
        4. LLM 生成回复（注入证据 + 预警上下文）
        """
        # Step 1: 提取数值（快速调用，低 token）
        values = await self._extract_values(message, session)

        # Step 2: 预警评估
        alert_level, alert_detail = self.alert_engine.evaluate(message, values)

        # Step 3: 按需检索（当患者提到新症状时）
        extra_evidences = []
        if self._needs_evidence_search(message):
            extra_evidences = await self.evidence.search_on_demand(message)

        # Step 4: 构造 LLM 上下文并生成回复
        system_prompt = self._build_system_prompt(session, alert_level)
        context = self._build_context(session, extra_evidences, values, alert_level)

        messages = [
            {"role": "system", "content": system_prompt},
            *self._history_to_messages(session.messages[-10:]),  # 最近 10 轮
            {"role": "user", "content": context + "\n\n患者说：" + message},
        ]

        response = await self.llm.chat_completion(
            messages=messages,
            temperature=0.5,
        )

        reply_text = response["content"]

        # Step 5: 更新会话状态
        user_msg = ChatMessage(role="user", content=message)
        assistant_msg = ChatMessage(
            role="assistant",
            content=reply_text,
            alert_level=alert_level,
            evidence_refs=[
                EvidenceRef(
                    source=ev.source,
                    title=ev.title,
                    snippet=(ev.abstract or "")[:100],
                    evidence_id=ev.id,
                )
                for ev in extra_evidences[:3]
            ],
        )
        session.messages.extend([user_msg, assistant_msg])
        session.current_alert = alert_level

        return ChatResponse(
            reply=reply_text,
            alert_level=alert_level,
            alert_detail=alert_detail,
            evidence_refs=assistant_msg.evidence_refs,
            suggest_actions=self._generate_actions(alert_level, values),
        )

    async def _extract_values(self, message: str, session: ChatSession) -> dict:
        """用 LLM 从消息中提取数值指标（轻量调用）"""
        prompt = f"""从以下患者消息中提取医学指标数值，返回 JSON：
{{"fasting_glucose": 数值或null, "postprandial_glucose": 数值或null,
  "blood_pressure_systolic": 数值或null, "blood_pressure_diastolic": 数值或null,
  "heart_rate": 数值或null, "temperature": 数值或null, "weight": 数值或null}}

只提取明确提到的数值，未提到的填 null。

患者消息："{message}"
"""
        try:
            resp = await self.llm.chat_completion(
                messages=[{"role": "user", "content": prompt}],
                response_format={"type": "json_object"},
                temperature=0.0,
            )
            raw = json.loads(resp["content"])
            return {k: v for k, v in raw.items() if v is not None}
        except Exception:
            return {}

    def _needs_evidence_search(self, message: str) -> bool:
        """判断是否需要补充检索"""
        search_triggers = [
            "不舒服", "疼痛", "头晕", "恶心", "失眠",
            "副作用", "过敏", "饮食", "能吃什么",
        ]
        return any(trigger in message for trigger in search_triggers)
```

### 3.5 KnowS 客户端封装 (knows_search.py)

```python
# app/tools/knows_search.py
# 复用 eb-consult 的封装，简化为独立版本

"""KnowS Evidence Search 客户端封装。

API: https://api.nullht.com/v1
Sources: paper_en, paper_cn, meeting, guide, trial, package_insert
Rate limit: 匿名 3 QPS / API Key 10 QPS
"""

import asyncio
import json
import os
from typing import Optional

import httpx

from app.models import Evidence, EvidenceSource

DEFAULT_API_ROOT = "https://api.nullht.com/v1"
SERIAL_INTERVAL = 0.4  # seconds

SOURCE_ENDPOINTS = {
    EvidenceSource.PAPER_EN: "/evidences/ai_search_paper_en",
    EvidenceSource.PAPER_CN: "/evidences/ai_search_paper_cn",
    EvidenceSource.MEETING: "/evidences/ai_search_meeting",
    EvidenceSource.GUIDE: "/evidences/ai_search_guide",
    EvidenceSource.TRIAL: "/evidences/ai_search_trial",
    EvidenceSource.PACKAGE_INSERT: "/evidences/ai_search_package_insert",
}


class KnowSClient:
    """KnowS 循证检索客户端"""

    def __init__(
        self,
        api_root: str = DEFAULT_API_ROOT,
        api_key: Optional[str] = None,
        timeout: float = 30.0,
    ):
        self.api_root = api_root.rstrip("/")
        self.api_key = api_key or os.getenv("KNOWS_API_KEY")
        self.timeout = timeout

    async def search(
        self,
        source: EvidenceSource,
        query: str,
        max_results: int = 10,
    ) -> list[Evidence]:
        """
        查询单个证据源。

        Args:
            source: 证据源类型
            query: 查询语句
            max_results: 最大返回条数

        Returns:
            Evidence 列表
        """
        endpoint = SOURCE_ENDPOINTS.get(source)
        if not endpoint:
            raise ValueError(f"Unsupported source: {source}")

        url = f"{self.api_root}{endpoint}"
        headers = {"Content-Type": "application/json"}
        if self.api_key:
            headers["Authorization"] = f"Bearer {self.api_key}"

        payload = {"query": query}

        async with httpx.AsyncClient(timeout=self.timeout) as client:
            resp = await client.post(url, json=payload, headers=headers)
            resp.raise_for_status()
            data = resp.json()

        return self._parse_response(data, source, max_results)

    async def search_multi(
        self,
        queries: list[tuple[EvidenceSource, str]],
        interval: float = SERIAL_INTERVAL,
    ) -> list[Evidence]:
        """
        串行查询多个源（避免限流）。

        Args:
            queries: [(source, query), ...]
            interval: 调用间隔（秒）

        Returns:
            所有源的证据合并列表
        """
        all_evidences = []
        for i, (source, query) in enumerate(queries):
            if i > 0:
                await asyncio.sleep(interval)
            try:
                results = await self.search(source, query)
                all_evidences.extend(results)
            except Exception as e:
                # 单源失败不阻断
                continue
        return all_evidences

    async def health_check(self) -> bool:
        """检查 KnowS API 是否可用"""
        try:
            await self.search(EvidenceSource.GUIDE, "test", max_results=1)
            return True
        except Exception:
            return False

    def _parse_response(
        self,
        data: dict,
        source: EvidenceSource,
        max_results: int,
    ) -> list[Evidence]:
        """解析 KnowS 响应"""
        evidences_raw = data.get("evidences", []) or []
        results = []
        for raw in evidences_raw[:max_results]:
            if not isinstance(raw, dict) or not raw.get("id"):
                continue
            results.append(Evidence(
                id=raw["id"],
                source=source,
                title=raw.get("title", ""),
                abstract=raw.get("abstract"),
                publish_date=raw.get("publish_date"),
                journal=raw.get("journal"),
                doi=raw.get("doi"),
                study_type=raw.get("study_type"),
                impact_factor=raw.get("impact_factor"),
                has_pdf=bool(raw.get("has_pdf", False)),
                organizations=raw.get("organizations", []) or [],
            ))
        return results
```

---

## 4. LLM 网关设计

### 4.1 统一接口（兼容 OpenAI / 通义千问 / DeepSeek）

```python
# app/tools/llm_gateway.py

"""LLM 统一调用网关。
支持 OpenAI、通义千问（DashScope）、DeepSeek 等兼容 OpenAI 格式的服务。
"""

import os
from typing import Optional
from openai import AsyncOpenAI


class LLMGateway:
    """LLM 统一网关"""

    # 预设 Provider 配置
    PROVIDERS = {
        "openai": {
            "base_url": "https://api.openai.com/v1",
            "default_model": "gpt-4o-mini",
        },
        "dashscope": {  # 通义千问
            "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1",
            "default_model": "qwen-plus",
        },
        "deepseek": {
            "base_url": "https://api.deepseek.com/v1",
            "default_model": "deepseek-chat",
        },
    }

    def __init__(
        self,
        provider: str = "dashscope",
        api_key: Optional[str] = None,
        model: Optional[str] = None,
    ):
        config = self.PROVIDERS.get(provider, self.PROVIDERS["dashscope"])
        self.model = model or config["default_model"]

        key = api_key or os.getenv(f"{provider.upper()}_API_KEY", "")

        self.client = AsyncOpenAI(
            api_key=key,
            base_url=config["base_url"],
        )

    async def chat_completion(
        self,
        messages: list[dict],
        temperature: float = 0.5,
        response_format: Optional[dict] = None,
        max_tokens: int = 4096,
    ) -> dict:
        """
        统一调用接口。

        Returns:
            {"content": "回复文本", "usage": {...}}
        """
        kwargs = {
            "model": self.model,
            "messages": messages,
            "temperature": temperature,
            "max_tokens": max_tokens,
        }
        if response_format:
            kwargs["response_format"] = response_format

        resp = await self.client.chat.completions.create(**kwargs)
        choice = resp.choices[0]

        return {
            "content": choice.message.content or "",
            "usage": {
                "prompt_tokens": resp.usage.prompt_tokens if resp.usage else 0,
                "completion_tokens": resp.usage.completion_tokens if resp.usage else 0,
            },
        }
```

### 4.2 配置切换 (.env)

```env
# LLM 配置（三选一，改 PROVIDER 即可切换）
LLM_PROVIDER=dashscope
DASHSCOPE_API_KEY=sk-xxx
OPENAI_API_KEY=sk-xxx
DEEPSEEK_API_KEY=sk-xxx

# KnowS 配置（可选，不填则匿名调用）
KNOWS_API_KEY=
KNOWS_BASE_URL=https://api.nullht.com/v1
```

---

## 5. API 路由设计

```python
# app/api/routes.py

from fastapi import APIRouter, HTTPException

router = APIRouter(prefix="/api")


@router.post("/parse", response_model=ParseResponse)
async def parse_discharge_summary(req: ParseRequest):
    """解析出院小结"""
    parser = get_parser_service()
    structured, confidence = await parser.parse(req.text)
    return ParseResponse(
        structured=structured,
        raw_text=req.text,
        confidence=confidence,
    )


@router.post("/plan", response_model=PlanResponse)
async def generate_followup_plan(req: PlanRequest):
    """生成随访计划（含循证检索）"""
    evidence_svc = get_evidence_service()
    planner_svc = get_planner_service()

    # 串行：先检索，再生成
    evidences = await evidence_svc.search_for_plan(req.structured)
    plan = await planner_svc.generate(req.structured, evidences)

    return PlanResponse(
        plan=plan,
        evidence_summary=f"共检索到 {plan.evidence_count} 条循证证据，引用 {plan.cited_count} 条",
    )


@router.post("/chat", response_model=ChatResponse)
async def chat_followup(req: ChatRequest):
    """对话式随访"""
    chatbot = get_chatbot_service()
    store = get_session_store()

    session = store.get(req.session_id)
    if not session:
        raise HTTPException(status_code=404, detail="Session not found")

    response = await chatbot.chat(session, req.message)
    store.save(session)

    return response


@router.post("/sessions", response_model=dict)
async def create_session(req: PlanRequest):
    """创建新会话（基于已有结构化数据）"""
    store = get_session_store()
    session = ChatSession(
        session_id=f"sess_{datetime.now().strftime('%Y%m%d%H%M%S')}",
        discharge_summary=req.structured,
    )
    store.save(session)
    return {"session_id": session.session_id}


@router.get("/sessions/{session_id}", response_model=ChatSession)
async def get_session(session_id: str):
    """获取会话详情"""
    store = get_session_store()
    session = store.get(session_id)
    if not session:
        raise HTTPException(status_code=404, detail="Session not found")
    return session


@router.get("/health", response_model=HealthResponse)
async def health_check():
    """健康检查"""
    knows = get_knows_client()
    knows_ok = await knows.health_check()
    return HealthResponse(
        status="ok" if knows_ok else "degraded",
        knows_api=knows_ok,
        llm_api=True,
    )
```

---

## 6. 前端设计（Streamlit）

### 6.1 页面结构

```python
# frontend/app.py

import streamlit as st

st.set_page_config(page_title="循证随访管家", page_icon="🏥", layout="wide")

# 侧边栏导航
page = st.sidebar.radio("导航", [
    "📋 上传出院小结",
    "📅 查看随访计划",
    "💬 对话随访",
])

if page == "📋 上传出院小结":
    render_upload_page()
elif page == "📅 查看随访计划":
    render_plan_page()
elif page == "💬 对话随访":
    render_chat_page()
```

### 6.2 各页面设计要点

**上传页面**
- 大文本框（st.text_area）粘贴出院小结
- 示例数据按钮（一键填充 Demo 数据）
- 解析结果以可编辑表格展示
- "生成随访计划" 按钮

**计划展示页面**
- 时间线组件（纵向排列，每个节点可展开）
- 任务卡片：图标 + 标题 + 详情 + 循证来源标签
- 证据面板：展示所有检索到的证据
- 顶部显示 "共检索 X 条证据，引用 Y 条"

**对话页面**
- 类聊天界面（st.chat_message + st.chat_input）
- 消息气泡上叠加预警颜色标识
- 证据引用以折叠块展示
- 顶部状态栏：当前预警等级（绿/黄/红）

---

## 7. 会话存储设计

```python
# app/store.py

"""JSON 文件会话存储（黑客松简化版）。"""

import json
from pathlib import Path

DATA_DIR = Path("data/sessions")


class SessionStore:
    """基于 JSON 文件的会话存储"""

    def __init__(self, data_dir: Path = DATA_DIR):
        self.data_dir = data_dir
        self.data_dir.mkdir(parents=True, exist_ok=True)

    def save(self, session: ChatSession) -> None:
        path = self.data_dir / f"{session.session_id}.json"
        path.write_text(session.model_dump_json(indent=2), encoding="utf-8")

    def get(self, session_id: str) -> Optional[ChatSession]:
        path = self.data_dir / f"{session_id}.json"
        if not path.exists():
            return None
        data = json.loads(path.read_text(encoding="utf-8"))
        return ChatSession(**data)

    def list_all(self) -> list[str]:
        return [p.stem for p in self.data_dir.glob("*.json")]
```

---

## 8. 关键时序图

### 8.1 主流程时序

```
用户          Streamlit        FastAPI         LLM          KnowS API
 │               │               │              │              │
 │──粘贴文本────▶│               │              │              │
 │               │──POST /parse─▶│              │              │
 │               │               │──结构化提取──▶│              │
 │               │               │◀─JSON结果────│              │
 │               │◀─解析结果─────│              │              │
 │──确认修改────▶│               │              │              │
 │               │──POST /plan──▶│              │              │
 │               │               │──查指南──────────────────────▶│
 │               │               │◀─指南结果─────────────────────│
 │               │               │──查说明书────────────────────▶│
 │               │               │◀─说明书结果───────────────────│
 │               │               │──查论文──────────────────────▶│
 │               │               │◀─论文结果────────────────────│
 │               │               │──融合生成───▶│              │
 │               │               │◀─计划JSON───│              │
 │               │◀─随访计划─────│              │              │
 │──开始对话────▶│               │              │              │
 │               │──POST /chat──▶│              │              │
 │               │               │──提取数值──▶│              │
 │               │               │──规则评估    │              │
 │               │               │──(可选)检索─────────────────▶│
 │               │               │──生成回复──▶│              │
 │               │               │◀─回复文本───│              │
 │               │◀─对话回复─────│              │              │
 │◀─展示回复────│               │              │              │
```

### 8.2 预警触发时序

```
患者消息 → ChatbotSvc.chat()
    │
    ├──▶ AlertEngine._extract_values()    # LLM 提取数值
    │
    ├──▶ AlertEngine.evaluate()           # 规则引擎判断
    │       ├── 关键词匹配 → RED
    │       ├── 数值超红色阈值 → RED
    │       ├── 数值超黄色阈值 → YELLOW
    │       └── 均正常 → GREEN
    │
    ├──▶ (如果 YELLOW/RED) EvidenceSvc.search_on_demand()
    │       # 补充检索相关证据
    │
    └──▶ LLM 生成回复（注入预警上下文）
            # 系统 prompt 中告知 AI 当前预警等级
            # AI 据此调整回复语气和建议
```

---

## 9. 依赖与环境

### 9.1 requirements.txt

```
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0
openai>=1.30.0
httpx>=0.27.0
streamlit>=1.35.0
python-dotenv>=1.0.0
```

### 9.2 运行命令

```bash
# 启动后端
cd followup-pilot
uvicorn app.main:app --reload --port 8000

# 启动前端（另一个终端）
cd followup-pilot
streamlit run frontend/app.py --server.port 8501
```

---

## 10. 测试策略

### 10.1 Demo 数据（3 个典型病例）

| 病例 | 诊断 | 特点 |
|------|------|------|
| 病例1 | 2型糖尿病 + 高血压 | 多药联用，指标监测 |
| 病例2 | 急性阑尾炎术后 | 术后恢复，饮食管理 |
| 病例3 | 社区获得性肺炎 | 抗生素疗程，复查影像 |

### 10.2 对话测试用例（病例1）

| 输入 | 预期预警 | 预期行为 |
|------|---------|---------|
| "今天空腹血糖 6.5" | GREEN | 肯定 + 继续监测 |
| "血糖有点高，8.5" | YELLOW | 建议调整饮食 + 关注趋势 |
| "血糖 16，而且头晕" | RED | 建议立即就医 |
| "最近总是心慌出汗" | YELLOW (关键词) | 询问详情 + 检索低血糖证据 |
| "一切都好，没什么不舒服" | GREEN | 肯定 + 提醒按时复查 |

---

## 11. 安全与合规

### 11.1 免责声明展示位置
- 前端页面顶部固定横幅
- 每次 AI 回复末尾自动附加
- 随访计划底部注明

### 11.2 安全边界
- AI 不给出具体药物剂量调整建议
- AI 不替代医生做诊断
- 红色预警必须明确建议就医
- 所有证据引用可追溯

### 11.3 数据处理
- 不存储患者真实身份信息（Demo 场景）
- 会话数据仅存本地 JSON 文件
- 不向第三方传输患者数据

---

## Part 2: 深度技术设计

> v2.0 新增 7 大深度模块，详见 [技术设计书-Part2-深度设计.md](技术设计书-Part2-深度设计.md)
>
> 包含：RAG Pipeline / 多 Agent 协作 / LangGraph 状态机 / 趋势预警 / 患者画像 / 证据分级 / 安全护栏
